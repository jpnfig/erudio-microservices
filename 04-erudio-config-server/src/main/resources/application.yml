server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:jpnfig/erudio-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'
